<div class="primary">primary</div>
<div class="secondary">secondary</div>
<div class="accent">accent</div>
<div class="error">error</div>
<div class="info">info</div>
<div class="success">success</div>
<div class="warning">warning</div>
<div class="lineHighlightBackground">lineHighlightBackground</div>
<div class="menu">menu</div>
<div class="sidebarNavigation">sidebarNavigation</div>
<div class="expandedSidebar">expandedSidebar</div>
<div class="toolbar">toolbar</div>
<div class="tabInactive">tabInactive</div>
<div class="sidebarSelection">sidebarSelection</div>
<div class="tabActive">tabActive</div>
<div class="background">background</div>
<div class="footer">footer</div>
<div class="scrollbarThumb">scrollbarThumb</div>
<div class="behaviorPack">behaviorPack</div>
<div class="resourcePack">resourcePack</div>
<div class="skinPack">skinPack</div>
<div class="worldTemplate">worldTemplate</div>

<!-- To-Do:
1. Help Button for manifest and settings

2. Fix abandoned_castle_feature_rule subpack children not working

X. Optional Changes:
    - Bulk Operations
        - Select multiple files with checkboxes to delete/duplicate/export at once
        - "Select All" / "Select None" buttons in each group
    - Search Enhancements
        - Search within file contents (not just names)
        - Search by file path/subpack
        - Regular expression search support
    - Quick Actions Menu
        - Right-click context menu on groups for bulk actions
        - "Open all in subpack" option
        - "Copy all identifiers" for quick reference
    - Validation & Linting
        - Real-time JSON validation as you edit manifest
        - Highlight duplicate UUIDs across files
        - Warn about common issues (missing dependencies, invalid references)
    - Scan Optimization
        - "Incremental scan" - only rescan changed files
        - Background auto-scan with file watcher
        - Scan specific subpacks only (partial scan)
    - File Organization
        - Drag files between subpacks in the UI
        - "Move to subpack" bulk action
        - Visual indication of empty folders
    - Export/Import Improvements
        - Export entire subpack as zip
        - Import from zip with structure preservation
        - Template system (save/load common structures)
    - Statistics & Analytics
        - File size statistics
        - Dependency graph visualization
        - "Orphaned files" detection (files not referenced anywhere)
    - UI Enhancements
        - Keyboard shortcuts (Ctrl+F for search, F5 for scan, etc.)
        - Collapsible sidebar sections
        - Custom column sorting (by name, size, errors, subpack)
        - Dark/light theme toggle
    - Developer Tools
        - Export scan results as JSON for external tools
        - Compare two scans (diff view)
        - Performance profiling (which files slow down scan)
    - Copy to clipboard buttons - For identifiers, file paths, UUIDs
    - "Scan on save" - Auto-scan when manifest is saved
    - Last scan timestamp - Show when last scan completed
    - Filter persistence - Remember filter settings between sessions
    - Keyboard navigation - Arrow keys to navigate file lists
-->